{
  "name": "gh-pages",
  "version": "1.0.0",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "rm -rf node_modules/.cache && next build && next export && touch out/.nojekyll && git add out/ && git commit -m \"Deploy satm\" && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "next": "^7.0.2",
    "react": "^16.8.1",
    "react-dom": "^16.8.1"
  },
  "license": "ISC",
  "devDependencies": {
    "babel-plugin-transform-define": "^1.3.0"
  }
}
